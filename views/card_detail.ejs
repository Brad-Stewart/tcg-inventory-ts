<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Card Details - PackRat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🐀 PackRat</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/"><i class="fas fa-home"></i> Dashboard</a>
                <a class="nav-link" href="/add_card"><i class="fas fa-plus"></i> Add Card</a>
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user"></i> <%= session.user_email %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if (flash && flash.error && flash.error.length > 0) { %>
            <% flash.error.forEach(message => { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <%= message %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% }); %>
        <% } %>
        <% if (flash && flash.success && flash.success.length > 0) { %>
            <% flash.success.forEach(message => { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= message %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% }); %>
        <% } %>

        <div class="row">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body text-center">
                        <% if (card.image_url) { %>
                            <img src="<%= card.image_url %>" class="img-fluid mb-3" alt="<%= card.card_name %>" style="max-width: 100%;">
                        <% } else { %>
                            <div class="bg-light d-flex align-items-center justify-content-center mb-3" style="height: 300px;">
                                <span class="text-muted">No Image Available</span>
                            </div>
                        <% } %>
                        
                        <% if (card.image_url_back) { %>
                            <img src="<%= card.image_url_back %>" class="img-fluid" alt="<%= card.card_name %> (Back)" style="max-width: 100%;">
                        <% } %>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h3 class="mb-0"><%= card.card_name %></h3>
                        <div class="btn-group">
                            <button type="button" class="btn btn-outline-primary" data-bs-toggle="modal" data-bs-target="#editModal">
                                <i class="fas fa-edit"></i> Edit
                            </button>
                            <form method="POST" action="/delete_card/<%= card.id %>" style="display: inline;">
                                <button type="submit" class="btn btn-outline-danger"
                                        onclick="return confirm('Are you sure you want to delete this card?')">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </form>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <table class="table table-sm">
                                    <tr>
                                        <td><strong>Set Name:</strong></td>
                                        <td><%= card.set_name || 'Unknown' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Set Code:</strong></td>
                                        <td><%= card.set_code || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Collector Number:</strong></td>
                                        <td><%= card.collector_number || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Rarity:</strong></td>
                                        <td>
                                            <% if (card.rarity) { %>
                                                <span class="badge 
                                                    <%= card.rarity === 'Common' ? 'bg-secondary' : 
                                                        card.rarity === 'Uncommon' ? 'bg-success' :
                                                        card.rarity === 'Rare' ? 'bg-warning' :
                                                        card.rarity === 'Mythic' ? 'bg-danger' : 'bg-dark' %>">
                                                    <%= card.rarity %>
                                                </span>
                                            <% } else { %>
                                                -
                                            <% } %>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td><strong>Colors:</strong></td>
                                        <td><%= card.colors || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Mana Cost:</strong></td>
                                        <td><%= card.mana_cost || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Mana Value:</strong></td>
                                        <td><%= card.mana_value || 0 %></td>
                                    </tr>
                                </table>
                            </div>
                            <div class="col-md-6">
                                <table class="table table-sm">
                                    <tr>
                                        <td><strong>Quantity:</strong></td>
                                        <td><%= card.quantity || 1 %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Condition:</strong></td>
                                        <td><%= card.condition || 'Near Mint' %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Foil:</strong></td>
                                        <td>
                                            <% if (card.is_foil) { %>
                                                <span class="badge bg-warning">Yes</span>
                                            <% } else { %>
                                                No
                                            <% } %>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td><strong>Purchase Price:</strong></td>
                                        <td>$<%= (card.purchase_price || 0).toFixed(2) %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Current Price:</strong></td>
                                        <td>$<%= (card.current_price || 0).toFixed(2) %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Total Value:</strong></td>
                                        <td>$<%= (card.total_value || 0).toFixed(2) %></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Price Change:</strong></td>
                                        <td>
                                            <% 
                                            const priceChange = card.price_change || 0;
                                            const priceClass = priceChange > 0 ? 'text-success' : 
                                                              priceChange < 0 ? 'text-danger' : 'text-muted';
                                            %>
                                            <span class="<%= priceClass %>">
                                                <%= priceChange > 0 ? '+' : '' %>$<%= priceChange.toFixed(2) %>
                                            </span>
                                        </td>
                                    </tr>
                                </table>
                            </div>
                        </div>

                        <% if (card.card_type) { %>
                            <div class="row mt-3">
                                <div class="col-12">
                                    <strong>Card Type:</strong> <%= card.card_type %>
                                </div>
                            </div>
                        <% } %>

                        <% if (card.market_url) { %>
                            <div class="row mt-3">
                                <div class="col-12">
                                    <a href="<%= card.market_url %>" target="_blank" class="btn btn-info">
                                        <i class="fas fa-external-link-alt"></i> View on Scryfall
                                    </a>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Modal -->
    <div class="modal fade" id="editModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Edit Card</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form method="POST" action="/edit_card/<%= card.id %>">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label for="quantity" class="form-label">Quantity</label>
                            <input type="number" class="form-control" id="quantity" name="quantity" 
                                   min="1" value="<%= card.quantity || 1 %>">
                        </div>
                        <div class="mb-3">
                            <label for="condition" class="form-label">Condition</label>
                            <select class="form-select" id="condition" name="condition">
                                <option value="Near Mint" <%= card.condition === 'Near Mint' ? 'selected' : '' %>>Near Mint</option>
                                <option value="Lightly Played" <%= card.condition === 'Lightly Played' ? 'selected' : '' %>>Lightly Played</option>
                                <option value="Moderately Played" <%= card.condition === 'Moderately Played' ? 'selected' : '' %>>Moderately Played</option>
                                <option value="Heavily Played" <%= card.condition === 'Heavily Played' ? 'selected' : '' %>>Heavily Played</option>
                                <option value="Damaged" <%= card.condition === 'Damaged' ? 'selected' : '' %>>Damaged</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="purchase_price" class="form-label">Purchase Price</label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input type="number" class="form-control" id="purchase_price" name="purchase_price" 
                                       min="0" step="0.01" value="<%= (card.purchase_price || 0).toFixed(2) %>">
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="alert_threshold" class="form-label">Price Alert Threshold</label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input type="number" class="form-control" id="alert_threshold" name="alert_threshold" 
                                       min="0" step="0.01" value="<%= (card.price_alert_threshold || 0).toFixed(2) %>">
                            </div>
                            <div class="form-text">Set a price threshold to get alerts when the card's value changes significantly.</div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>